interface:
  display_name: "Decompose Spec"
  short_description: "Plan domain tasks for new or existing specs"
  default_prompt: "Use $decompose-spec to read SPEC.md when no source is supplied and write the domain interface plan and implementation tasks to TASKS.md."
